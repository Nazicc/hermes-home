# gateway/runners — extracted sub-modules from GatewayRunner (run.py God Object)
#
# S1: utils.py   — GatewayUtils (session keys, voice mode, session info, media delivery)
# S2: cache.py   — AgentCache   (agent caching, session overrides)
# S3: enricher.py — EnricherManager (content enrichment pipeline)
