---
name: claude-code
description: "Delegate coding tasks to Claude Code (Anthropic's CLI agent). Use for building features, refactoring, PR reviews, and iterative coding. Also use when performing batch YAML frontmatter updates across multiple files — adding 'Use when'/'NOT for' trigger patterns to SKILL.md descriptions, fixing malformed frontmatter blocks, or replacing description content across a skills directory. Best for iterative patch-verify workflows where subagent patches may not persist. Requires the claude CLI installed. NOT for: JSON file edits, non-YAML configs, or when a single write_file operation is sufficient."
category: skills
---


