# Honcho Configuration - Self-hosted with MiniMax LLM backend

[app]
LOG_LEVEL = "INFO"
SESSION_OBSERVERS_LIMIT = 10
GET_CONTEXT_MAX_TOKENS = 8000
MAX_FILE_SIZE = 5242880
MAX_MESSAGE_SIZE = 25000
EMBED_MESSAGES = true
NAMESPACE = "honcho"

[db]
CONNECTION_URI = "postgresql+psycopg://honcho:honcho123@honcho-postgres:5432/honcho"
SCHEMA = "public"
POOL_PRE_PING = true
POOL_SIZE = 5
MAX_OVERFLOW = 10

[auth]
USE_AUTH = false

[llm]
DEFAULT_MAX_TOKENS = 4096
MAX_TOOL_OUTPUT_CHARS = 10000
MAX_MESSAGE_CONTENT_CHARS = 2000
OPENAI_API_KEY = "REDACTED-MINIMAX"

[embedding]
VECTOR_DIMENSIONS = 1024

[embedding.model_config]
transport = "openai"
model = "BAAI/bge-large-zh-v1.5"

[embedding.model_config.overrides]
base_url = "https://api.siliconflow.cn/v1"
api_key = "sk-owuhzyvbvboldcpjtfnptvmyqqhncyxeypqcrbsrwofjzyzh"

[deriver]
ENABLED = true
WORKERS = 1
POLLING_SLEEP_INTERVAL_SECONDS = 1.0
STALE_SESSION_TIMEOUT_MINUTES = 5
DEDUPLICATE = true
FLUSH_ENABLED = true
REPRESENTATION_BATCH_MAX_TOKENS = 128

[deriver.model_config]
transport = "openai"
model = "deepseek-v4-flash"

[deriver.model_config.overrides]
base_url = "https://api.deepseek.com/v1"
api_key = "REDACTED-DEEPSEEK"

[dialectic]

[dialectic.levels.minimal]
MAX_TOOL_ITERATIONS = 1
TOOL_CHOICE = "auto"

[dialectic.levels.minimal.model_config]
transport = "openai"
model = "deepseek-v4-flash"

[dialectic.levels.minimal.model_config.overrides]
base_url = "https://api.deepseek.com/v1"
api_key = "REDACTED-DEEPSEEK"

[dialectic.levels.low]
MAX_TOOL_ITERATIONS = 3
TOOL_CHOICE = "auto"

[dialectic.levels.low.model_config]
transport = "openai"
model = "deepseek-v4-flash"

[dialectic.levels.low.model_config.overrides]
base_url = "https://api.deepseek.com/v1"
api_key = "REDACTED-DEEPSEEK"

[dialectic.levels.medium]
MAX_TOOL_ITERATIONS = 2

[dialectic.levels.medium.model_config]
transport = "openai"
model = "deepseek-v4-flash"

[dialectic.levels.medium.model_config.overrides]
base_url = "https://api.deepseek.com/v1"
api_key = "REDACTED-DEEPSEEK"

[dialectic.levels.high]
MAX_TOOL_ITERATIONS = 4

[dialectic.levels.high.model_config]
transport = "openai"
model = "deepseek-v4-flash"

[dialectic.levels.high.model_config.overrides]
base_url = "https://api.deepseek.com/v1"
api_key = "REDACTED-DEEPSEEK"

[dialectic.levels.max]
MAX_TOOL_ITERATIONS = 10

[dialectic.levels.max.model_config]
transport = "openai"
model = "deepseek-v4-flash"

[dialectic.levels.max.model_config.overrides]
base_url = "https://api.deepseek.com/v1"
api_key = "REDACTED-DEEPSEEK"

[summary]
ENABLED = true
MESSAGES_PER_SHORT_SUMMARY = 20
MESSAGES_PER_LONG_SUMMARY = 60

[summary.model_config]
transport = "openai"
model = "deepseek-v4-flash"

[summary.model_config.overrides]
base_url = "https://api.deepseek.com/v1"
api_key = "REDACTED-DEEPSEEK"

[dream]
ENABLED = false
DOCUMENT_THRESHOLD = 50

[peer_card]
ENABLED = true

[vector_store]
TYPE = "pgvector"
DIMENSIONS = 1024
NAMESPACE = "honcho"
